export interface Student {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    dateOfBirth: string; // Use ISO date string or another format as per your needs
    department: string;
  }
  